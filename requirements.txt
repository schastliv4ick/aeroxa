fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
httpx>=0.27
openai>=1.55
# Newer than the pinned qdrant image, which only costs a cosmetic version warning.
# Do not "fix" that by bumping the image: see the note in docker-compose.yml.
qdrant-client>=1.12
FlagEmbedding>=1.3
torch>=2.4
PyYAML>=6.0
asyncpg>=0.30
